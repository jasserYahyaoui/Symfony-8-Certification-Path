import React from 'react';
import TrainingMock from '@site/src/components/TrainingMock';

/** Mock 1 — Knowledge (Master Plan §10). */
export default function Mock1(): React.JSX.Element {
  return <TrainingMock file="mock-1.json" heading="Mock 1 — Knowledge" />;
}
