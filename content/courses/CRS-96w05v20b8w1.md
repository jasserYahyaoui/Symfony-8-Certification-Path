---
id: CRS-96w05v20b8w1
official_item: OIT-hcdrp2y7kbct
title: "Custom callback validators"
content_level: STANDARD
language: fr
verification_status: VERIFIED
reviewed_at: "2026-09-01"
official_sources:
  - url: "https://raw.githubusercontent.com/symfony/symfony-docs/8.0/reference/constraints/Callback.rst"
    branch: "8.0"
    verified_at: "2026-09-01"
---

## Objectif

Écrire une règle de validation arbitraire sans créer de classe de contrainte, et
connaître la signature attendue — elle change selon que la méthode est
d'instance ou statique.

## Le principe

`Callback` est une contrainte de **classe** : elle désigne une méthode que le
validateur appelle, à charge pour elle de signaler ce qui ne va pas. C'est la
porte de sortie quand aucune contrainte native n'exprime la règle.

```php
#[Assert\Callback]
public function validate(ExecutionContextInterface $context, mixed $payload): void
{
    if (in_array($this->firstName, $this->fakeNames, true)) {
        $context->buildViolation('Ce prénom semble inventé.')
            ->atPath('firstName')
            ->addViolation();
    }
}
```

L'attribut se pose directement sur la méthode. Sur la classe, il faut nommer la
méthode : `#[Assert\Callback(callback: 'validate')]`.

## Les deux signatures

C'est le piège de l'item. Une méthode **d'instance** a `$this`, donc elle reçoit
le contexte en premier. Une méthode **statique** ne l'a pas, donc elle reçoit
l'objet validé en premier et le contexte ensuite :

| Forme | Signature |
|---|---|
| instance | `validate(ExecutionContextInterface $context, mixed $payload)` |
| statique | `validate(mixed $value, ExecutionContextInterface $context, mixed $payload)` |

L'ordre des deux premiers arguments s'inverse. Se tromper produit une erreur de
type, pas une validation silencieusement fausse.

Un callback peut aussi vivre **hors de la classe validée** : la contrainte
accepte alors un appelable statique,
`callback: [Acme\Validator::class, 'validate']`.

## `$payload`

Le troisième argument porte la donnée arbitraire attachée à la contrainte par
son option `payload`. Il n'est **pas** utilisé par le validateur lui-même : il
sert à transmettre un contexte applicatif — un niveau de gravité, par exemple.

## Ce que le callback doit faire

Rien de particulier s'il est satisfait : **ne pas ajouter de violation est la
façon de dire que c'est valide**. Le retour de la méthode est ignoré — retourner
`false` ne signale rien.

## Pièges d'examen

**La signature statique commence par la valeur, pas par le contexte.**

**Le retour est ignoré.** Une règle non respectée doit produire une violation,
pas un `return false`.

**`Callback` est une contrainte de classe** : elle voit tout l'objet, ce qui la
rend adaptée aux règles croisées — mais il faut alors `atPath()` pour attribuer
l'erreur au bon champ.

## Points clés

- `#[Assert\Callback]` sur la méthode, ou `callback:` pour la nommer.
- Instance : `(context, payload)`. Statique : `(value, context, payload)`.
- Un appelable externe statique est accepté.
- Aucune violation ajoutée = valide ; le retour n'est pas lu.

## Sources officielles

- [Contrainte `Callback`](https://raw.githubusercontent.com/symfony/symfony-docs/8.0/reference/constraints/Callback.rst)
